1 git:
2 config:
  - db:
    - application variables for db connection (?)
    - for integration tests (postgres/ h2)
3 semantics:
  - user entity:
      - password hashing prior to persistence
4 logic:
  - /register
  - /login
  - email after registration for confirmation
  - spring validation via spring-boot-starter-validation (tbc if needed)

wishlist:
  - kafka queue to notification-service with creation/update/deletion
  - kafka queue to present-service to delete presents
  - aspects:
      - on user creation
      - on user update
      - on user deletion

