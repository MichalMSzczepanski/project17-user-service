1 git:
2 config:
  - db:
    - application variables for db connection (?)
3 semantics:
  - password hashing prior to user persistence
4 logic:
  - /register
  - /login
  - email after registration for confirmation
  - /activate
  - spring validation via spring-boot-starter-validation (tbc)

wishlist:
  - kafka queue to notification-service with creation/update/deletion
  - kafka queue to present-service to delete presents
  - aspects:
      - on user creation
      - on user update
      - on user deletion

